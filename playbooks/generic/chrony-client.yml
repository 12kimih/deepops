---
# Chrony will ensure that the clocks of all your servers are in sync and can be used to sync with an internal server
- hosts: all
  become: true
  tasks:
    - name: Configure Chrony client
      ansible.builtin.include_role:
        name: DeepOps.chrony
