---
# System-wide locale applied to every node so multibyte (UTF-8) text renders
# consistently and shells never fall back to C/POSIX (which garbles UTF-8, e.g.
# CJK text shows up as mojibake). C.UTF-8 is always available and needs no language
# pack; a full locale like en_US.UTF-8 (the default) adds language-specific
# collation/messages.
locale_lang: "en_US.UTF-8"
