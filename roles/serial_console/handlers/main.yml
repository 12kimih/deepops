---
# grub-mkconfig builds to grub.cfg.new and syntax-checks it before moving it into place,
# so a configuration that fails to build never replaces the one the node boots from.
- name: Update grub
  ansible.builtin.command: "{{ serial_console_grub_update }}"
  changed_when: true
