---
- hosts: all
  become: true
  tasks:
    - name: Provide a grub defaults file to edit
      ansible.builtin.copy:
        dest: /etc/default/grub
        content: |
          GRUB_TIMEOUT_STYLE=hidden
          GRUB_TIMEOUT=0
          GRUB_CMDLINE_LINUX=""
        mode: "0644"
